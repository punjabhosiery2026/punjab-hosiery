import {NextResponse} from 'next/server';import {z} from 'zod';import {supabaseServer} from '@/lib/supabase/server';
async function admin(req:Request){const token=req.headers.get('authorization')?.replace('Bearer ','');if(!token)return null;const db=supabaseServer(),user=await db.auth.getUser(token);if(!user.data.user)return null;const profile=await db.from('profiles').select('role').eq('id',user.data.user.id).single();return profile.data?.role==='admin'?db:null}
const Status=z.enum(['new','contacted','quote_sent','converted','closed']);
export async function PATCH(req:Request){const db=await admin(req);if(!db)return NextResponse.json({error:'Unauthorized'},{status:401});const parsed=z.object({id:z.string().uuid(),status:Status}).safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'Invalid enquiry status'},{status:400});const result=await db.from('wholesale_inquiries').update({status:parsed.data.status}).eq('id',parsed.data.id).select().single();return result.error?NextResponse.json({error:result.error.message},{status:400}):NextResponse.json(result.data)}
